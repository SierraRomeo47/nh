# Auth Service

Authentication service (Auth, MFA, JWT boundary).

- Status: scaffold only, no business logic.
- Responsibilities: user auth, token issuance/refresh, MFA flows (to be added later).
- Gateway route: `/auth` via API gateway.
