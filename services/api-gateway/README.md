# API Gateway

HTTP entrypoint and reverse proxy, with request logging.

- Status: scaffold only, no business logic.
- Responsibilities: route to services, enforce edge policies, structured request logging.
