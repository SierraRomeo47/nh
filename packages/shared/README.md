# Shared Package

Types, schemas, and utilities that can be used across workspaces.

- Status: scaffold only, no business logic.
- Rules: no shared mutable state; prefer schema contracts over shared DTOs.
